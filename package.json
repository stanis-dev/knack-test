{
  "name": "knack",
  "version": "1.0.0",
  "description": "Knack is a no-code platform that includes an online database. Knack users will at times, through unexpected API usage or an unknown bug, corrupt their application schemas. One common issue they may run into is having duplicate fields and/or objects in their application schema. These duplicates cannot be removed by the Knack UI and lead to TypeErrors and other problems.",
  "main": "index.ts",
  "scripts": {
    "start": "nodemon index.ts --ignore './clean_application.json'",
    "test": "mocha --require ts-node/register ./src/tests/**/*.spec.ts --watch ./src/**/*.ts"
  },
  "author": "Stan",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.3.4",
    "@types/mocha": "^10.0.1",
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {}
}
